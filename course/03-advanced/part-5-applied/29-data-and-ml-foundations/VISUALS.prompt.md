# Visual Companion Prompt — Module 29: Data and ML Foundations

## Video Steering Prompt
> Create a high-detail animation illustrating memory architecture in numerical computing. Visualize a Python list of boxed PyObjects in fragmented heap memory vs a NumPy contiguous buffer with byte strides in cache lines. Animate 2D array broadcasting expanding virtual singleton dimensions across rows and columns. Illustrate data leakage in cross-validation splits and how scikit-learn `Pipeline` encapsulates transformation parameters exclusively within training folds.

## Key Concepts
- Contiguous memory buffers, dtypes, and strides
- Array views vs memory copies in slicing
- Vectorization and SIMD instruction execution
- Broadcasting rules across multidimensional tensors
- pandas DataFrame internal columnar blocks and categorical encoding
- scikit-learn Estimator API and Pipeline encapsulation against data leakage
