# Virtual environment exercise

### Lesson
A virtual environment isolates package installs. Create one for each project and
install packages into that environment instead of the system Python.

### Task
Create a venv, install a package, and record the dependencies.
