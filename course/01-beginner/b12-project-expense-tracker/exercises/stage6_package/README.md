# Stage 6 package plan

### Lesson
Split the project into modules: `models.py`, `storage.py`, `reporting.py`, and a
`main.py` guard. This prevents one growing file from becoming unmaintainable.

### Task
Create the package structure and keep entrypoint logic separate from the model.
