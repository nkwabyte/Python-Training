# Solutions & Commentary — Module M07: Neural Networks

## Key Takeaways
- Non-linear activations (ReLU, GELU) allow multi-layer networks to approximate non-linear functions (Universal Approximation Theorem).
- Subclass `torch.nn.Module` and implement `forward()`.
- Optimizers: SGD with Momentum and AdamW with decoupled weight decay.
