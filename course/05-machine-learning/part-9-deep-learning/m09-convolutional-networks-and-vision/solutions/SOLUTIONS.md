# Solutions & Commentary — Module M09: Convolutional Networks and Vision

## Key Takeaways
- Convolutions leverage spatial locality and translation invariance via weight sharing.
- Shape math: Output spatial size $O = \lfloor \frac{W - K + 2P}{S} \rfloor + 1$.
- Transfer learning: Freeze pre-trained backbone weights and fine-tune classifier heads.
