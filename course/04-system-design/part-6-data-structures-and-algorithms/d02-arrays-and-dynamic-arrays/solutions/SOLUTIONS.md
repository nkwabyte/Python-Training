# Solutions & Commentary — Module D02: Arrays and Dynamic Arrays

## Key Takeaways
- Python `list` is a dynamic array of pointers with amortized $O(1)$ append and $O(N)$ insertion/deletion at index 0.
- Use two-pointer and sliding-window techniques for $O(N)$ array transformations.
