# Solutions & Commentary — Module D06: Graphs

## Key Takeaways
- Graph representations: Adjacency list is optimal for sparse graphs; adjacency matrix is optimal for dense graphs.
- BFS computes unweighted shortest paths; DFS finds connected components and detects cycles.
- Topological Sort via Kahn's algorithm or DFS post-order traversal for dependency resolution.
