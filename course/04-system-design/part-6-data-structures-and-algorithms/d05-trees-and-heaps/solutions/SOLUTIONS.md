# Solutions & Commentary — Module D05: Trees and Heaps

## Key Takeaways
- Binary Search Tree (BST) invariant: left subtree values < node value < right subtree values.
- In-order traversal of a BST yields elements in sorted ascending order.
- Python `heapq` provides min-heap operations (`heappush`, `heappop`) in $O(\log N)$ time.
