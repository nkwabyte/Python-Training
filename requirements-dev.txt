# Core development toolchain for the whole course.
# Install with:  uv pip install -r requirements-dev.txt
#            or: python -m pip install -r requirements-dev.txt

ruff>=0.6
mypy>=1.11
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.100
ipython>=8.20
rich>=13.7

# Later parts add their own; each module's README says what it needs.
# Part 5 preview (install when you get there):
#   httpx  fastapi  uvicorn  pydantic  sqlalchemy  alembic
#   numpy  pandas  matplotlib  scikit-learn
